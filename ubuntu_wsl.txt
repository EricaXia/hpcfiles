cd "/mnt/c/Users/erica/OneDrive - University of Southern California/Projects_at_USC/CKIDS_current/data"

- Install instaloader via pip
- Test instaloader command line tool
- Navigate to OneDrive data folder
- execute instaloader commands in a LOOP for all hashtags, see below

BASH:
- list should look like ("item1" "item2" "item3" "etc") , with elements in quotes and separated by spaces, NOT COMMAS!!!

examples of loops in BASH:
```
#!/bin/bash
for i in 1 2 3 4 5
do
   echo "Welcome $i times"
done
```

```
#!/bin/bash
for (( c=1; c<=5; c++ ))
do  
   echo "Welcome $c times"
done
```


In current directory
---
Counting num of posts (by .json post info):
ls -f *.json | wc -l

Count total num files:
ls -f | wc -l


